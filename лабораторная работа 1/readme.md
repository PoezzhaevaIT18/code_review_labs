ЛАБОРАТОРНАЯ РАБОТА ПО КОДУ РЕВЬЮ №1
-------------------------------------------------------
Исправления кода по требованиям качества и стилю Google Style Guide для C++.
-------------------------------------------------------
Лабораторная номер 1
-------------------------------------------------------
1 задание
-------------------------------------------------------
Археолог нашел N артефактов.Известны веса(сi) и объемы(di) артефактов.Нужно
выбрать такое подмножество найденных вещей, чтобы суммарный их вес находился в пределах
от A до B кг, а их общий объем оказался минимальным.Известно, что решение единственно.
Укажите порядковые номера вещей, которые надо взять.Исходный данные находятся в текстовом 
файле, в первой строке указаны N, А и B, а во второй строке значения весов(в кг), в третьей
объемы находок(в куб.см).Вывести так же суммарный вес и объем результата.

laba1.cpp
1)Удалена конструкция using namespace std  
Использование глобального пространства имён нарушает Google Style.  
Все обращения заменены на std::.

2)Добавлена проверка открытия входного файла  
Ранее программа не проверяла, существует ли файл.  
Теперь при отсутствии файла выводится сообщение об ошибке и выполнение прекращается.

3)Добавлена проверка корректности входных параметров  
Проверяются значения n, A, B.  
Ранее программа могла работать с отрицательными значениями или некорректным диапазоном.

4)Добавлена проверка корректности весов артефактов  
Вес каждого предмета должен быть строго > 0.  
Ранее программа принимала нулевые и отрицательные значения.

5)Добавлена проверка корректности объёмов артефактов  
Аналогично весам: объём должен быть строго > 0.

6)Переименован массив dp в min_volume  
Название стало более понятным и отражает назначение переменной.

7)Добавлен комментарий к обратному циклу динамического программирования  
Пояснено, почему цикл идёт от большего веса к меньшему —  
чтобы не перезаписать данные текущей итерации.

8)Исправлена логика восстановления решения  
Добавлена проверка min_volume[w - weights[i]] != INT_MAX,  
чтобы не включать предметы, которые невозможно использовать.

9)Добавлена проверка корректности чтения данных из файла  
Если файл повреждён или содержит недостаточно чисел, программа выводит ошибку.

10)Улучшены сообщения об ошибках  
Теперь выводятся понятные и точные сообщения, указывающие причину сбоя.

-------------------------------------------------------

Header.h
1)Добавлены корректные include guards  
Используются #ifndef / #define / #endif для защиты от повторного включения.

2)Структура ArtifactResult оформлена по стилю  
Поля структурированы, имена соответствуют назначению, типы подобраны корректно.

3)Вынесены объявления функций  
solve_artifacts корректно объявлена в заголовочном файле.

-------------------------------------------------------

Общее
1)Код приведён к единому стилю  
Используются std::, аккуратное форматирование, понятные имена переменных.

2)Повышена устойчивость программы  
Добавлены проверки ошибок на всех этапах: чтение файла, чтение данных, восстановление решения.

3)Добавлены поясняющие комментарии  
Ключевые участки алгоритма снабжены комментариями, объясняющими назначение и логику.

-------------------------------------------------------
2 задание
-------------------------------------------------------
Черепашка.На квадратной доске расставлены целые неотрицательные числа, каждое из которых 
не превосходит 100. Черепашка, находящаяся в правом верхнем углу, мечтает попасть в левый нижний.
При этом она может переползать только в клетку слева или снизу и хочет, чтобы сумма всех чисел, 
оказавшихся у нее на пути, была бы максимальной.Определить эту сумму.Ввод и вывод организовать 
при помощи текстовых файлов.Формат входных данных : в первой строке входного файла записано 
число N - размер доски(1 < N < 80).Далее следует N строк, каждая из которых содержит N целых чисел,
представляющих доску.В выходной файл нужно вывести единственное число : максимальную сумму.

Source.cpp
1)Удалён using namespace std  
Использование глобального пространства имён нарушает Google Style.  
Все обращения заменены на std::.

2)Добавлена проверка открытия входного файла  
Ранее программа не проверяла, существует ли файл.  
Теперь при отсутствии файла выводится сообщение об ошибке.

3)Добавлена проверка корректности n  
Ранее программа принимала любые значения.  
Теперь проверяется условие 1 < n < 80.

4)Добавлена проверка корректности элементов матрицы  
Ранее программа принимала любые числа.  
Теперь элементы должны быть в диапазоне 0…100.

5)Добавлена проверка корректности чтения данных  
Если файл содержит меньше чисел, чем требуется, выводится ошибка.

6)Исправлена инициализация DP  
dp заполняется нулями, а стартовая точка задаётся корректно: dp[0][n-1].

7)Добавлены комментарии к логике динамического программирования  
Пояснено, почему первая строка заполняется движением влево,  
а первый столбец — движением вниз.

8)Исправлена логика вычисления dp[i][j]  
Используется максимум из двух возможных направлений: сверху и справа.

9)Добавлена запись результата в выходной файл  
Ранее программа могла не записать результат корректно.

10)Улучшены сообщения об ошибках  
Теперь выводятся понятные и точные сообщения, указывающие причину сбоя.

-------------------------------------------------------

Header2.h
1)Добавлены корректные include guards  
Используются #ifndef / #define / #endif для защиты от повторного включения.

2)Структура TurtleResult оформлена по стилю  
Поле max_sum отражает назначение структуры.

3)Вынесено объявление solve_turtle  
Функция корректно объявлена в заголовочном файле.

-------------------------------------------------------

main.cpp
1)Упрощён вызов solve_turtle  
Лишняя логика отсутствует, код стал чище.

2)Добавлен setlocale  
Корректный вывод русских сообщений в консоль.

3)Улучшена читаемость  
Файл содержит только запуск решения, без лишней логики.

-------------------------------------------------------

Общее
1)Код приведён к единому стилю  
Используются std::, аккуратное форматирование, понятные имена переменных.

2)Повышена устойчивость программы  
Добавлены проверки ошибок на всех этапах: открытие файла, чтение данных, проверка диапазонов.

3)Добавлены поясняющие комментарии  
Ключевые участки алгоритма снабжены комментариями, объясняющими назначение и логику.

-------------------------------------------------------
3 задание
-------------------------------------------------------
К - ичные числа.Среди чисел в системе счисления с основанием K(2≤K≤10) определить сколько 
имеется чисел из N(1 < N < 20, N + K < 26) разрядов таких, что в их записи содержится более 
трех подряд идущих нулей.Для того, чтобы избежать переполнения, ответ представьте в виде 
вещественного числа.

main.cpp
1)Добавлены проверки корректности входных данных  
Проверяются диапазоны K (2–10) и N (2–19).  
Ранее программа принимала любые значения.

2)Добавлена проверка условия N + K < 26  
Ранее программа не контролировала ограничение задачи.

3)Улучшены сообщения об ошибках  
Теперь выводятся точные и понятные причины некорректного ввода.

4)Повышена устойчивость программы  
При ошибке программа корректно завершает выполнение.

-------------------------------------------------------
solve_k_numbers.cpp
1)Добавлена проверка корректности входных данных  
Ранее функция не проверяла параметры и могла работать с некорректными значениями.  
Теперь проверяются диапазоны K, N и условие N + K < 26.

2)Исправлена логическая ошибка в формуле перехода  
Ранее использовалось умножение на k, что неверно.  
Исправлено на (k − 1), так как только (k − 1) цифр являются ненулевыми.

3)Добавлены комментарии к алгоритму  
Пояснено, что отслеживаются состояния по количеству нулей в конце числа:  
0, 1, 2, 3 и ≥4.

4)Исправлена логика переходов между состояниями  
Теперь корректно обновляются nz, oz, tz, thz и foz.

5)Исправлена формула для состояния foz  
Ранее учитывалось только добавление нуля.  
Теперь учитывается и переход из состояния ≥4 нулей при добавлении любой цифры.

6)Улучшена читаемость кода  
Введены временные переменные _nz, _oz, _tz, _thz, _foz для сохранения предыдущего состояния.

-------------------------------------------------------
Header3.h
1)Добавлены корректные include guards  
Используются #ifndef / #define / #endif для защиты от повторного включения.

2)Структура ZeroResult оформлена по стилю  
Поле count отражает назначение структуры.

3)Вынесено объявление solve_k_numbers  
Функция корректно объявлена в заголовочном файле.

-------------------------------------------------------

Общее
1)Код приведён к единому стилю  
Используются std::, аккуратное форматирование, понятные имена переменных.

2)Повышена устойчивость программы  
Добавлены проверки ошибок на всех этапах: ввод, валидация, вычисления.

3)Добавлены поясняющие комментарии  
Ключевые участки алгоритма снабжены комментариями, объясняющими назначение и логику.

-------------------------------------------------------
4 задание
-------------------------------------------------------
Во всех заданиях данной подгруппы предполагается, что исходные строки, определяющие выражения, 
не содержат пробелов.При выполнении заданий не следует использовать оператор цикла.
Вывести значение логического выражения, заданного в виде строки S.Выражение определяется следующим 
образом(«T» — True, «F» — False) :<выражение> :: = T | F | And(<выражение>, <выражение>) | Or(<выражение>, <выражение>)

Source4.cpp
1)Удалён using namespace std  
Использование глобального пространства имён нарушает Google Style.  
Все обращения заменены на std::.

2)Добавлена проверка недопустимых символов  
Функция IsValidExpression теперь проверяет, что строка содержит только  
T, F, A, n, d, O, r, (, ), , и не содержит пробелов.  
Ранее программа принимала любые символы.

3)Исправлена логика обработки неверных фрагментов  
Ранее неверные фрагменты выражения интерпретировались как F.  
Теперь они считаются ошибкой и приводят к завершению вычисления.

4)Добавлена проверка, что подвыражение действительно разобрано  
В Eval введены переменные before_left и before_right.  
Если рекурсивный вызов не продвинул индекс i — это ошибка.  
Ранее Or(T,) ошибочно давал результат T.

5)Добавлена проверка наличия запятой в And и Or  
Ранее выражения вида And(TT) или Or(TT) не считались ошибкой.  
Теперь отсутствие запятой приводит к ошибке.

6)Добавлена проверка закрывающей скобки  
Ранее выражения And(T,F или Or(T,F не считались ошибкой.  
Теперь отсутствие ')' корректно обрабатывается.

7)Улучшена читаемость кода  
Добавлены комментарии, поясняющие структуру грамматики и логику рекурсии.

-------------------------------------------------------

main.cpp
1)Добавлена проверка корректности строки  
Если строка содержит пробелы или недопустимые символы — выводится "Ошибка!".

2)Добавлена проверка результата Eval  
Если Eval вернул false, но исходная строка не равна "F",  
значит произошла синтаксическая ошибка.

3)Добавлена проверка полного разбора строки  
Если после вычисления индекс i не равен длине строки,  
значит в выражении есть лишние символы.

4)Улучшены сообщения об ошибках  
Теперь программа выводит единое сообщение "Ошибка!" для всех некорректных случаев.

-------------------------------------------------------

Header4.h
1)Добавлены корректные include guards  
Используются #ifndef / #define / #endif для защиты от повторного включения.

2)Вынесены объявления функций Eval и IsValidExpression  
Ранее функции не были объявлены в заголовочном файле,  
что нарушало структуру проекта.

3)Заголовочный файл приведён к единому стилю  
Минимальный набор include, аккуратное форматирование.

-------------------------------------------------------

Общее
1)Код приведён к единому стилю  
Используются std::, аккуратное форматирование, понятные имена переменных.

2)Повышена устойчивость программы  
Добавлены проверки ошибок на всех этапах: валидация строки, разбор выражения,  
контроль полного разбора.

3)Исправлены логические ошибки в рекурсивном разборе  
Теперь программа корректно обрабатывает все некорректные выражения  
и не путает ошибку с логическим значением F.

4)Добавлены поясняющие комментарии  
Ключевые участки алгоритма снабжены комментариями, объясняющими назначение и логику.

-------------------------------------------------------
